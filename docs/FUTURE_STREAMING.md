# Future streaming

Streaming will resolve a media ID server-side, prefer HTTP range Direct Play, then remux, then bounded HLS transcoding. Sessions will be cancellable, local-network only, authenticated, and isolated from original files.

