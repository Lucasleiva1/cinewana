# Future local API

A later Axum adapter can expose versioned health, catalog, artwork, progress, favorites, player-control, and device endpoints over the existing services. The adapter must map domain models to redacted DTOs and require paired-device authorization.

