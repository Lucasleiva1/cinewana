# Future Android application

Android will consume opaque catalog DTOs and player-state contracts from the Windows services. It will not receive Windows paths. No mobile project exists in this stage.

